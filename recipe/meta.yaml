{% set name = "Ipopt" %}
{% set version = "3.12.10" %}
{% set sha256 = "8e0b1469e0fdbd4e64188a1645de8ac58087d74312dff6dce5bf5ab132f138f1" %}
{% set variant = "openblas" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: http://www.coin-or.org/download/pkgsource/{{ name }}/{{ name }}-{{ version }}.tgz
  sha256: {{ sha256 }}
  patches:
    - ipopt.patch

build:
  number: 0
  skip: True  # [win]
  number: 0
  features:
    - blas_{{ variant }}

requirements:
  build:
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - ampl-mp
    - mumps
    - metis
    - scotch
    - blas 1.1 {{ variant }}
    - openblas

  run:
    - ampl-mp
    - mumps
    - metis
    - scotch
    - blas 1.1 {{ variant }}
    - openblas

test:
  files:
    - test
    - mytoy.nl

  requires:
    - toolchain

about:
  home: https://projects.coin-or.org/Ipopt/
  license: EPL 1.0
  license_file: LICENSE
  summary: Software package for large-scale nonlinear optimization

extra:
  recipe-maintainers:
    - pstjohn
    - bluescarni
