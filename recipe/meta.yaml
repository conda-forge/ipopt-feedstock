{% set name = "Ipopt" %}
{% set version = "3.12.12" %}
{% set sha256 = "7baeb713ef8d1999bed397b938e9654b38ad536406634384455372dd7e4ed61f" %}
{% set variant = "openblas" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: http://www.coin-or.org/download/pkgsource/{{ name }}/{{ name }}-{{ version }}.tgz
  sha256: {{ sha256 }}

build:
  skip: True  # [win]
  number: 1001
  features:
    - blas_{{ variant }}

requirements:
  build:
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - ampl-mp
    - mumps
    - metis
    - scotch
    - blas 1.1 {{ variant }}
    - openblas

  run:
    - ampl-mp
    - mumps
    - metis
    - scotch
    - blas 1.1 {{ variant }}
    - openblas

test:
  files:
    - test
    - mytoy.nl

  requires:
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}

about:
  home: https://projects.coin-or.org/Ipopt/
  license: EPL 1.0
  license_file: LICENSE
  summary: Software package for large-scale nonlinear optimization

extra:
  recipe-maintainers:
    - pstjohn
    - bluescarni
